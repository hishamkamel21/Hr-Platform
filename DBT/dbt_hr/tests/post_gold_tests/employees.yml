version: 2 

models: 
  - name : dim_employees 

    columns: 

      - name: department_id 
        tests: 
          - relationships: 
              to: ref('dim_departments')
              field: department_id 
    
      - name: job_id 
        tests: 
          - relationships: 
              to: ref('dim_jobs')
              field: job_id 